<%= form_with(model: vaga_de_emprego) do |form| %>
  <% if vaga_de_emprego.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vaga_de_emprego.errors.count, "error") %> prohibited this vaga_de_emprego from being saved:</h2>

      <ul>
        <% vaga_de_emprego.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :titulo, style: "display: block" %>
    <%= form.text_field :titulo %>
  </div>

  <div>
    <%= form.label :descricao, style: "display: block" %>
    <%= form.text_area :descricao %>
  </div>

  <div>
    <%= form.label :salario, style: "display: block" %>
    <%= form.text_field :salario %>
  </div>

  <div class="field">
    <%= form.label :empregador_id %>
    <%= form.collection_select(:empregador_id, Empregador.all, :id, :cnpj, {:selected => @vaga_de_emprego.empregador_id})%>
  </div>

  <div>
    <%= form.submit "Criar Vaga", class: "btn btn-primary btn-lg" %>
  </div>
<% end %>
