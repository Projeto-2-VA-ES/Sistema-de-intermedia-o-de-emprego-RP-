<p style="color: green"><%= notice %></p>

<h1>Candidaturas</h1>

<%= link_to "Voltar para a página inicial", root_path %>

<div id="candidaturas">
  <% @candidaturas.each do |candidatura| %>
    <%= render candidatura %>
    <p>
      <%= link_to "Show this candidatura", candidatura %>
    </p>
  <% end %>
</div>

<%= link_to "New candidatura", new_candidatura_path, class: "btn btn-primary btn-lg float-left" %>
