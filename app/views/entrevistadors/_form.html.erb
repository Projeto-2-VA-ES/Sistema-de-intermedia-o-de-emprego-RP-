<%= form_with(model: entrevistador) do |form| %>
  <% if entrevistador.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(entrevistador.errors.count, "error") %> prohibited this entrevistador from being saved:</h2>

      <ul>
        <% entrevistador.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label :telefone, style: "display: block" %>
    <%= form.text_field :telefone %>
  </div>

  <div>
    <%= form.label :vaga_de_emprego_id, style: "display: block" %>
    <%= form.select :vaga_de_emprego_id, options_for_select(VagaDeEmprego.all.map{ |v| [v.titulo, v.id] }) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
